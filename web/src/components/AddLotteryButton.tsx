import { Fab, Icon } from "@mui/material";

export default function AddLotteryButton() {
  return (
    <Fab variant="extended" className="add-lottery-button">
      <Icon>add</Icon>
      ADD LOTTERY
    </Fab>
  );
}